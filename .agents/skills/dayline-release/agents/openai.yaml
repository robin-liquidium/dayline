interface:
  display_name: "Dayline Release"
  short_description: "Cut a notarized Dayline release from a clean main"
  default_prompt: "Use $dayline-release to run the Dayline release workflow — changelog, tag, notarized GitHub release, website, Sparkle, and Homebrew — and report exactly which stages completed and which remain pending (such as notarization continuation)."
