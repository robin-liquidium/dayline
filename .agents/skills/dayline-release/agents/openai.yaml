interface:
  display_name: "Dayline Release"
  short_description: "Cut a notarized Dayline release from a clean main"
  default_prompt: "Use $dayline-release to update the changelog, tag main, complete the notarized GitHub, website, Sparkle, and Homebrew release, and verify every distribution path."
