interface:
  display_name: "Dayline PR"
  short_description: "Review and land Dayline work on main without releasing"
  default_prompt: "Use $dayline-pr to review all current Dayline changes, fix legitimate findings, and create a clean PR targeting main. Merge only when explicitly authorized or when invoked by an authorized release workflow. Do not release."
